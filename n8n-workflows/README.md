# N8N Автоматизация для Фриланс-бирж

Полностью автоматизированная система для работы с фриланс-биржами (Kwork, FL.ru, LinkedIn, Profi.ru, YouDo, Telegram каналы).

## Возможности

### 1. Автоматический анализ заданий
- Определение типа задачи (разработка, дизайн, текст, другое)
- Оценка сложности (1-10)
- Выявление требуемых навыков
- Оценка реалистичности бюджета
- Обнаружение "красных флагов"
- Рекомендация: брать или не брать задание

### 2. Генерация откликов с самопроверкой
- Self-correction loop: до 3 итераций улучшения
- Проверка на соответствие ТЗ
- Оценка конкретности и профессионализма
- Автоматическое улучшение на основе ревью

### 3. Выполнение IT-задач
- Генерация плана реализации
- Написание кода с комментариями
- Автоматическое код-ревью
- Проверка на баги и безопасность
- Создание README с инструкциями

### 4. Помощь с дизайн-задачами
- Подробные инструкции для новичков
- Промпты для AI генераторов (Leonardo.ai, Bing Image Creator)
- Техзадания для Canva/Figma
- Чеклисты проверки качества

## Используемые бесплатные AI модели

**Текущая модель по умолчанию:** `qwen/qwen3-coder:free`

Эта модель используется для всех AI операций:
- Анализ заданий
- Генерация откликов
- Код-ревью
- Генерация дизайн-инструкций

**Альтернативные бесплатные модели через OpenRouter:**

1. **Qwen 3 Coder** - `qwen/qwen3-coder:free` (по умолчанию)
   - Хорошо подходит для IT-задач
   - Стабильный rate limit

2. **Meta Llama 3.2 3B** - `meta-llama/llama-3.2-3b-instruct:free`
   - Быстрая модель
   - Подходит для простых задач

3. **Mistral 7B Instruct** - `mistralai/mistral-7b-instruct:free`
   - Хорошо генерирует тексты
   - Отлично для откликов

4. **Google Gemini Flash 1.5** - `google/gemini-flash-1.5:free`
   - Мощная модель
   - Может иметь строгий rate limit

Все модели доступны бесплатно через [OpenRouter.ai](https://openrouter.ai/)

**Важно:** Бесплатные модели имеют rate limits (лимиты запросов). Workflow настроен с задержками между запросами для избежания ошибок 429.

## Установка и настройка

### Шаг 1: Установите n8n

```bash
npm install -g n8n
```

Или используйте Docker:

```bash
docker run -it --rm \
  --name n8n \
  -p 5678:5678 \
  -v ~/.n8n:/home/node/.n8n \
  n8nio/n8n
```

### Шаг 2: Получите API ключи

#### OpenRouter API Key (БЕСПЛАТНО)
1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
2. Перейдите в раздел "Keys"
3. Создайте новый API ключ
4. **ВАЖНО**: Free tier предоставляет ограниченное количество запросов. Для production используйте платный план или alternative API.

#### Telegram Bot Token (БЕСПЛАТНО)
1. Найдите в Telegram бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Сохраните токен вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### Шаг 3: Импортируйте workflow

1. Откройте n8n (обычно `http://localhost:5678`)
2. Нажмите "Add workflow" → "Import from file"
3. Выберите файл `freelance-automation-master.json`
4. Workflow будет импортирован

### Шаг 4: Настройте credentials

#### OpenRouter API
1. В n8n перейдите в "Credentials" → "Add Credential"
2. Выберите "HTTP Header Auth"
3. Создайте credential с именем "OpenRouter API Key":
   - Header Name: `Authorization`
   - Header Value: `Bearer ВАШ_API_КЛЮЧ_OPENROUTER`

#### Telegram Bot API
1. В n8n перейдите в "Credentials" → "Add Credential"
2. Выберите "Telegram API"
3. Введите токен от BotFather

### Шаг 5: Активируйте workflow

1. Откройте импортированный workflow
2. Нажмите "Active" в правом верхнем углу
3. Workflow начнёт слушать сообщения от Telegram бота

## Использование

### Формат сообщения боту

Отправьте боту сообщение в следующем формате:

```
Новый заказ
Биржа: Kwork
Ссылка: https://kwork.ru/projects/12345
Заголовок: Нужен парсер Wildberries
Описание: Требуется написать Python скрипт для парсинга товаров с Wildberries. Необходимо собирать название, цену, рейтинг, количество отзывов. Данные сохранять в Excel.
Бюджет: 5000₽
Дедлайн: 3 дня
```

### Минимальный формат

Можно отправить просто описание задачи, остальные поля необязательны:

```
Описание: Нужен Telegram бот для учёта расходов с кнопками добавления категорий
```

### Что происходит после отправки

1. **Подтверждение** - Бот пришлёт сообщение "Задание принято в обработку"
2. **Анализ** (10-30 сек) - AI анализирует задание и отправляет результат:
   - Тип задачи
   - Сложность
   - Требуемые навыки
   - Оценка бюджета
   - Красные флаги (если есть)
   - Рекомендация: брать или не брать

3. **Генерация отклика** (20-60 сек) - Если рекомендация "брать":
   - Генерируется черновик отклика
   - AI проверяет качество
   - При необходимости улучшает (до 3 итераций)
   - Отправляет финальный вариант с оценкой

4. **Выполнение задачи** (30-120 сек) - В зависимости от типа:

   **Для IT-задач:**
   - Генерируется план реализации
   - Пишется код с комментариями
   - Проводится код-ревью
   - Создаётся README
   - Отправляется готовое решение

   **Для дизайн-задач:**
   - Создаётся подробная инструкция
   - Генерируются промпты для AI-генераторов
   - Даётся техзадание для Canva/Figma
   - Предоставляется чеклист проверки

## Структура workflow

```
Telegram Trigger
    ↓
Parse Message (парсинг входящего сообщения)
    ↓
Check Error (проверка валидности)
    ├→ Send Error (если ошибка)
    └→ Send Acknowledgement (подтверждение)
        ↓
    AI Analyze Task (анализ с Google Gemini)
        ↓
    Parse Analysis (обработка результата)
        ↓
    Send Analysis (отправка анализа)
        ↓
    Check Recommendation (если рекомендация "брать")
        ↓
    Generate Draft (Mistral 7B)
        ↓
    Review Draft (Google Gemini) ←┐
        ↓                          │
    Check Improvement Needed       │
        ├→ [loop до 3 раз] ────────┘
        └→ Send Final Response
            ↓
        ├→ IT Task Path (Llama 3.1)
        │   ├→ Generate Code
        │   ├→ Review Code
        │   └→ Send Code
        │
        └→ Design Task Path (Google Gemini)
            ├→ Generate Design Guide
            └→ Send Design Guide
```

## Примеры использования

### Пример 1: Задача на парсинг

**Вход:**
```
Биржа: FL.ru
Описание: Нужен парсер для сбора контактов компаний с 2GIS
Бюджет: 3000₽
```

**Анализ:**
- Тип: разработка
- Сложность: 6/10
- Навыки: Python, requests, BeautifulSoup/Selenium
- Бюджет: средняя реалистичность
- Рекомендация: БРАТЬ

**Отклик (пример):**
> Здравствуйте! Готов выполнить парсинг 2GIS. Опыт: 3+ года Python, создавал парсеры для Avito, Wildberries, аналогичных площадок. Использую requests + BeautifulSoup/Selenium в зависимости от структуры сайта. Сбор данных с обработкой пагинации и anti-bot защиты. Выгрузка в Excel/CSV. Срок: 2-3 дня. Уточните: какие именно города и категории нужны?

**Код (фрагмент):**
```python
import requests
from bs4 import BeautifulSoup
import pandas as pd
# ... полный код с обработкой ошибок, README, примерами
```

### Пример 2: Дизайн логотипа

**Вход:**
```
Биржа: Kwork
Описание: Логотип для кофейни "Утро", минималистичный стиль
Бюджет: 1500₽
```

**Анализ:**
- Тип: дизайн
- Сложность: 4/10
- Навыки: графический дизайн, работа с векторами
- Рекомендация: БРАТЬ

**Инструкция (фрагмент):**
> **АНАЛИЗ ЗАДАЧИ:**
> Нужен минималистичный логотип для кофейни "Утро". Стиль: современный, чистый, ассоциации с утром и кофе.
>
> **ИНСТРУМЕНТЫ:**
> 1. Canva (для быстрого создания)
> 2. Leonardo.ai (для генерации иконок)
>
> **ПРОМПТ ДЛЯ AI:**
> "Minimalist coffee shop logo, morning theme, coffee cup with sunrise, simple lines, modern, vector style, beige and brown colors"
>
> **ПОШАГОВАЯ ИНСТРУКЦИЯ:**
> 1. Откройте Canva → Создать дизайн → Логотип (500x500px)
> 2. Используйте шрифт: Montserrat (жирный для "УТРО")
> 3. Цвета: #D4A574 (беж), #6F4E37 (коричневый)
> ...

## Лимиты и ограничения

### Бесплатные API (OpenRouter)

**Rate Limits для free tier моделей:**
- **Qwen 3 Coder:** ~10-20 запросов/мин
- **Llama 3.2:** ~5-10 запросов/мин
- **Mistral 7B:** ~5-10 запросов/мин
- **Google Gemini:** ~10-20 запросов/мин

**Встроенная защита от rate limits:**
- Задержки 3-5 секунд между AI-запросами
- Автоматические повторные попытки (3 раза)
- Обработка ошибок 429 с информативными сообщениями

**Рекомендации:**
- Не отправляйте больше 1-2 заданий в минуту
- Если видите ошибку 429, подождите 1-2 минуты
- Для интенсивного использования подключите платный план OpenRouter ($5-20/мес)

**Для продакшена рекомендуется:**
1. Использовать платный план OpenRouter (убирает все лимиты)
2. Увеличить задержки в Wait нодах до 10 секунд
3. Настроить fallback на альтернативные модели
4. Добавить очередь обработки заданий

### Telegram Bot
- Максимальная длина сообщения: 4096 символов
- Для длинных ответов (код, инструкции) сообщения разбиваются на части

## Расширенная конфигурация

### Изменение AI моделей

**Как сменить модель:**

1. Откройте workflow в n8n
2. Найдите нужный HTTP Request узел (например, "AI Analyze Task")
3. В параметрах найдите `Body Parameters` → `model`
4. Измените значение на желаемую модель

**Текущая модель:** `qwen/qwen3-coder:free`

**Рекомендуемые бесплатные альтернативы:**

Для всех задач (универсальные):
- `qwen/qwen3-coder:free` (используется сейчас, хорошо для IT)
- `google/gemini-flash-1.5:free` (мощная, но строгий rate limit)
- `meta-llama/llama-3.2-3b-instruct:free` (быстрая, легкая)

Для генерации откликов (лучшие текстовые модели):
- `mistralai/mistral-7b-instruct:free` (отличные тексты)
- `qwen/qwen3-coder:free` (текущая)

Для кода (лучшие для программирования):
- `qwen/qwen3-coder:free` (специализирована для кода)
- `deepseek/deepseek-coder-6.7b-instruct:free` (если доступна)

**Платные модели (высокое качество, без лимитов):**
- `openai/gpt-4-turbo` (~$0.01-0.03 за запрос)
- `anthropic/claude-3-sonnet` (~$0.003-0.015 за запрос)
- `google/gemini-1.5-pro` (~$0.001-0.005 за запрос)

### Настройка промптов

Промпты находятся в узлах HTTP Request в параметре `messages`. Вы можете:
- Изменить описание своего опыта
- Добавить конкретные кейсы
- Изменить стиль откликов (более формальный/неформальный)
- Добавить специфические требования для вашей ниши

### Добавление уведомлений

Можете добавить узлы для отправки уведомлений в:
- Email (SMTP узел)
- Slack
- Discord
- Webhooks

## Troubleshooting

### Бот не отвечает
1. Проверьте, что workflow активирован (зелёная галочка)
2. Проверьте credentials для Telegram
3. Откройте "Executions" и посмотрите логи ошибок

### Ошибка "The service is receiving too many requests from you" (429)

Эта ошибка возникает при превышении лимита бесплатных запросов к OpenRouter API.

**Решения:**

1. **Подождите 1-2 минуты** - бесплатные модели имеют rate limit, который сбрасывается через некоторое время

2. **Используйте другую бесплатную модель:**
   - `qwen/qwen3-coder:free` (рекомендуется, используется по умолчанию)
   - `meta-llama/llama-3.2-3b-instruct:free`
   - `mistralai/mistral-7b-instruct:free`
   - `google/gemini-flash-1.5:free`

3. **Увеличьте задержки между запросами:**
   - В workflow уже добавлены Wait ноды (3-5 секунд)
   - Если ошибка повторяется, увеличьте время ожидания в нодах "Wait Before Analyze", "Wait Before Draft", "Wait Before Review"
   - Рекомендуемые значения: 5-10 секунд

4. **Используйте платный план OpenRouter:**
   - От $5/мес для стабильной работы
   - Нет rate limits
   - Доступны более мощные модели (GPT-4, Claude, и др.)

5. **Настройте retry логику:**
   - Workflow уже настроен на 3 попытки с задержкой 5 секунд
   - При постоянных ошибках проверьте лимиты на [openrouter.ai/settings](https://openrouter.ai/settings)

**Временное решение:**
Если workflow не работает из-за rate limit:
- Используйте workflow реже (не более 1-2 заданий в минуту)
- Отключите некоторые AI-ноды (например, код-ревью или self-correction loop)
- Переключитесь на другую модель в параметрах HTTP Request нод

### Ошибки AI запросов
1. Проверьте OpenRouter API key
2. Проверьте баланс на OpenRouter (для free tier)
3. Попробуйте другую модель (могут быть временные проблемы)
4. Проверьте header "Authorization: Bearer YOUR_KEY" в credentials

### Некорректные ответы
1. Улучшите промпты (добавьте больше контекста)
2. Увеличьте `maxIterations` в self-correction loop
3. Используйте более мощные модели (платные)

## Best Practices

1. **Проверяйте отклики перед отправкой** - AI может ошибаться, всегда читайте финальный вариант
2. **Дополняйте анализ своим опытом** - AI не знает специфику конкретных заказчиков
3. **Не автоматизируйте отправку** - используйте workflow как помощника, а не замену
4. **Обновляйте промпты** - добавляйте успешные кейсы в промпты
5. **Следите за лимитами** - бесплатные API имеют ограничения

## Дополнительные ресурсы

- [Документация n8n](https://docs.n8n.io/)
- [OpenRouter документация](https://openrouter.ai/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Примеры промптов](./PROMPTS.md)

## Поддержка

Если возникли вопросы или проблемы, создайте issue в репозитории или напишите в обсуждения.

## Лицензия

MIT License - используйте свободно, на свой риск.

---

**Важно:** Этот workflow предназначен для помощи в работе, а не для полной автоматизации. Всегда проверяйте результаты AI перед отправкой заказчикам. Фриланс требует человеческого подхода и ответственности.
